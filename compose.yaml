services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      FRANCHISEOPS_DB: /app/data/franchiseops.db
    volumes:
      - franchise_data:/app/data
    restart: unless-stopped
volumes:
  franchise_data:
